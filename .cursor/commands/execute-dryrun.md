# Execute Cleanup (Dry Run)

Preview what changes would be made without actually modifying the system.

```powershell
.\scripts\execute_cleanup.ps1 -DryRun
```

Optional: Specify level with `-Level light|moderate|aggressive`
